{
  "name": "@franz-lola/pixel-renderer",
  "version": "0.0.0-monorepo",
  "description": "Shared pixel-art renderer for Franz & Lola games and tools.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "vite build",
    "test": "node --test",
    "benchmark": "node scripts/render-benchmark.mjs",
    "benchmark:assert": "node scripts/render-benchmark.mjs --assert",
    "test:browser": "node scripts/browser-regression.mjs",
    "verify": "npm test && npm run build && npm run benchmark:assert && npm run test:browser"
  },
  "devDependencies": {
    "playwright": "^1.54.2",
    "vite": "^6.4.3"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.8",
    "@franz-lola/content-model": "0.0.0-monorepo",
    "@franz-lola/game-core": "0.0.0-monorepo"
  }
}