{
  "name": "@franz-lola/publisher",
  "version": "0.0.0-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply LEVEL_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply LEVEL_DB --remote",
    "deploy": "wrangler deploy --keep-vars",
    "types": "wrangler types",
    "test": "node --test"
  },
  "dependencies": {
    "@franz-lola/pixel-renderer": "0.0.0-monorepo"
  },
  "devDependencies": {
    "wrangler": "4.118.0"
  },
  "overrides": {
    "undici": "7.29.0"
  },
  "engines": {
    "node": ">=22.14.0"
  }
}
